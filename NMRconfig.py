# Config file for the full nmrfreq program
# config file is editable through the main program
# nmrfreq -c FN <-- Returns to system default

# Sets accelerator environment
ACCELERATOR = 'FN'
OUTPUT = '~'
